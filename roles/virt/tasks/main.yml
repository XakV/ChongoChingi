- name: virt | Install virt packages
  dnf:
      state: latest
      name: "{{ item }}"
  with_items:
  - '@Virtualization'
  - podman
  - buildah
  - skopeo
  - vagrant
  - vagrant-libvirt
  - vagrant-sshfs
  - vagrant-hostmanager
